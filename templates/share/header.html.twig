
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <a class="navbar-brand" href="{{ path('home')}}">LittleBnb</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav me-auto">
            <li class="nav-item">
                <a class="nav-link active" href="{{ path('home')}}">Accueil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="{{ path('annonce')}}">Annonces</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="{{ path('annonce_create')}}">Créer une annonces</a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            <a class="nav-link" href="#">Mon compte</a>
        </ul>
        </div>
        </nav>

        {% for type, messages in app.flashes %}
            <div class="container mt-3">
                <div class="alert alert-{{ type }}">
                    {% for message in messages %}
                        <p> {{ message | raw }} </p>
                    {% endfor %}
                </div>
            </div>
        {% endfor %}